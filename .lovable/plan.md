سأصلح سبب بقاء صفحة الإدارة على شاشة الدخول بعد إدخال الهاتف والرمز الصحيحين.

الخطة:
1. تعديل إعدادات جلسة الإدارة حتى تكون الكوكي متوافقة مع بيئة المعاينة المنشورة، وليس localhost فقط.
2. جعل تسجيل الدخول يتحقق فورًا بعد نجاح العملية من أن الجلسة أصبحت فعالة قبل الانتقال إلى `/admin`.
3. تحسين التنقل بعد الدخول والخروج باستخدام `replace` حتى لا يرجع زر المتصفح إلى واجهة خاطئة.
4. إضافة رسالة خطأ واضحة إذا نجحت بيانات الدخول لكن لم تُحفظ الجلسة، بدل أن يبقى المستخدم على نفس الواجهة بدون تفسير.
5. التحقق عمليًا بعد التنفيذ من أن إدخال `07733570130` و `7979` ينقل إلى لوحة الطلبات.

التفاصيل التقنية:
- سأراجع `src/lib/admin-session.server.ts` و `src/lib/admin.functions.ts` و `src/routes/admin.login.tsx` فقط.
- المرجح أن الخلل من شرط `secure`/الكوكي أو من عدم انتظار تحقق الجلسة بعد `s.update()`، لأن طلب الدخول يرجع `ok: true` لكن فحص الإدارة بعدها يرجع غير مصادق.