# OmniRoute setup

يستخدم المشروع OmniRoute كمسار الذكاء الاصطناعي الأساسي، ويستخدم Lovable AI Gateway كمسار fallback تقني عند أخطاء الشبكة أو `408/425/429/5xx` من OmniRoute. أخطاء السياسة أو المصادقة أو النموذج غير الصالح لا يتم تجاوزها تلقائيًا إلى مزود آخر.

أضف المتغيرات التالية إلى بيئة الخادم:

```env
OMNIROUTE_BASE_URL=http://omniroute:20128/v1
OMNIROUTE_API_KEY=your-omniroute-key
LOVABLE_API_KEY=your-lovable-key
```

إذا كان OmniRoute يعمل على مضيف آخر، استبدل `OMNIROUTE_BASE_URL` بعنوانه الداخلي القابل للوصول من خادم التطبيق. لا تضع هذه المتغيرات في `VITE_*` ولا تعرضها للمتصفح.

جميع استدعاءات Chat وImages الحالية تمر عبر `src/lib/ai-gateway.server.ts`. اختيار النموذج يبقى في إعدادات `ai_models_config` والمُنسّق الحالي، بينما يرسل gateway الطلب أولًا إلى OmniRoute. تسجل أحداث التوليد المزود الفعلي في `generation_events.provider`.

طلبات وضع البالغين تحتاج تحقق العمر الموجود في النظام. وإذا أعلن المستخدم عن شخصية حقيقية، يتوقف الطلب حتى تصبح حالة الموافقة الإدارية `consent_approved`. لا يبدأ التوليد من واجهة المستخدم وحدها؛ الحاجز موجود أيضًا في الخادم قبل تأكيد الدفع وبدء الوظائف.
